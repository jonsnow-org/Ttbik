fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.11.7
supabase==2.31.0
groq==0.13.1
google-generativeai==0.8.3
huggingface_hub==0.31.4
chromadb==0.5.23
ddgs==9.16.0
python-dotenv==1.0.1
streamlit==1.41.1
requests==2.32.3
pypdf==5.1.0
python-docx==1.1.2

# Owner spec, 2026-09-12 (Cloudflare Workers AI integration —
# cloudflare_ai.py): real per-frame watermark/mux for video (ffmpeg via
# imageio-ffmpeg's bundled static binary, no system apt package needed)
# and real pixel-level watermark + PNG metadata for images (Pillow) —
# same two libraries modelscope-studio/requirements.txt already uses for
# the identical reason there, needed here too since main.py now does
# this post-processing itself for Cloudflare-sourced media.
Pillow==11.0.0
imageio-ffmpeg==0.5.1
